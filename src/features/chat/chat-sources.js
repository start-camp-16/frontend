export function renderChatSources(container,sources=[]){container.replaceChildren();for(const source of sources){if(source.type==='location'){const item=document.createElement('div');item.className='chat-source';const title=document.createElement('strong');title.textContent=source.title;const meta=document.createElement('span');meta.textContent=[source.category,source.district,source.address].filter(Boolean).join(' · ');item.append(title,meta);container.append(item)}else if(source.type==='post'){const link=document.createElement('a');link.className='chat-source';link.href=`/posts/${source.post_id}`;const title=document.createElement('strong');title.textContent=source.title;const meta=document.createElement('span');meta.textContent=source.tag;link.append(title,meta);container.append(link)}}}
