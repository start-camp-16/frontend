import {getComments,createComment,updateComment,deleteComment} from './board-api.js';import {validateComment} from './comment-validation.js';import {openModal} from '../../ui/modal.js';
export function mountComments({root,postId,signal}){root.innerHTML=`<h2>댓글</h2><form class="comment-form"><label>댓글<textarea name="content" maxlength="1000" rows="3"></textarea></label><label>비밀번호<input name="password" type="password" maxlength="20"></label><small role="alert"></small><button>댓글 남기기</button></form><div class="comments"></div>`;const form=root.querySelector('form'),list=root.querySelector('.comments');const load=async()=>{try{const data=await getComments(postId,{signal});list.replaceChildren();data.items.forEach(comment=>{const article=document.createElement('article');article.className='comment panel';const p=document.createElement('p');p.textContent=comment.content;const time=document.createElement('time');time.textContent=new Date(comment.created_at).toLocaleString('ko-KR');const edit=document.createElement('button');edit.type='button';edit.textContent='수정';edit.addEventListener('click',()=>editComment(comment,article));const del=document.createElement('button');del.type='button';del.textContent='삭제';del.addEventListener('click',()=>removeComment(comment,del));article.append(p,time,edit,del);list.append(article)})}catch(error){if(error.name!=='AbortError')list.textContent=error.message}};const editComment=(comment,article)=>{article.innerHTML=`<form><label>댓글<textarea name="content"></textarea></label><label>비밀번호<input name="password" type="password"></label><small role="alert"></small><button>수정 저장</button></form>`;const editForm=article.querySelector('form');editForm.elements.content.value=comment.content;editForm.addEventListener('submit',async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(editForm));const errors=validateComment(values);if(Object.keys(errors).length){editForm.querySelector('small').textContent=Object.values(errors)[0];return}try{await updateComment(comment.id,values,{signal});await load()}catch(error){editForm.querySelector('small').textContent=error.code==='PASSWORD_MISMATCH'?'비밀번호가 일치하지 않습니다.':error.message}})};const removeComment=(comment,trigger)=>{const input=document.createElement('input');input.type='password';input.placeholder='댓글 비밀번호';const error=document.createElement('small');const wrap=document.createElement('div');wrap.append(input,error);openModal({title:'댓글을 삭제할까요?',content:wrap,trigger,confirmLabel:'삭제',onConfirm:async()=>{try{await deleteComment(comment.id,{password:input.value},{signal});await load();return true}catch(e){error.textContent=e.code==='PASSWORD_MISMATCH'?'비밀번호가 일치하지 않습니다.':e.message;return false}}})};form.addEventListener('submit',async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(form));const errors=validateComment(values);if(Object.keys(errors).length){form.querySelector('small').textContent=Object.values(errors)[0];return}try{await createComment(postId,values,{signal});form.reset();await load()}catch(error){form.querySelector('small').textContent=error.message}});load();return()=>{};}
