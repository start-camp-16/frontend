import {getPost,deletePost} from './board-api.js';import {mountComments} from './comments-controller.js';import {openModal} from '../../ui/modal.js';import './board.css';
export function renderPostLoadError(root,message){const box=document.createElement('div');box.className='async-state';box.setAttribute('role','alert');const text=document.createElement('p');text.textContent=message;const link=document.createElement('a');link.href='/posts';link.textContent='게시판으로';box.append(text,link);root.replaceChildren(box)}
export function mountPostDetailPage({outlet,params,signal,navigate}){outlet.innerHTML=`<div id="post-detail"></div><section id="comments-root"></section>`;const root=outlet.querySelector('#post-detail');getPost(params.id,{signal}).then(post=>{const article=document.createElement('article');article.className='post-detail panel';article.innerHTML=`<span class="post-tag"></span><h1></h1><div class="post-meta"></div><p class="post-content"></p><div class="post-actions"><a class="button button--secondary" href="/posts/${post.id}/edit">수정</a><button type="button">삭제</button></div>`;article.querySelector('.post-tag').textContent=[post.district,post.prefix].filter(Boolean).join(' · ');article.querySelector('h1').textContent=post.title;article.querySelector('.post-content').textContent=post.content;article.querySelector('.post-meta').textContent=`작성 ${new Date(post.created_at).toLocaleString('ko-KR')}${post.updated_at!==post.created_at?` · 수정 ${new Date(post.updated_at).toLocaleString('ko-KR')}`:''}`;article.querySelector('button').addEventListener('click',e=>removePost(e.currentTarget));root.replaceChildren(article);mountComments({root:outlet.querySelector('#comments-root'),postId:params.id,signal})}).catch(error=>{if(error.name!=='AbortError')renderPostLoadError(root,error.message)});const removePost=trigger=>{const input=document.createElement('input');input.type='password';input.placeholder='게시글 비밀번호';const error=document.createElement('small');const wrap=document.createElement('div');wrap.append(input,error);openModal({title:'게시글을 삭제할까요?',content:wrap,trigger,confirmLabel:'삭제',onConfirm:async()=>{try{await deletePost(params.id,{password:input.value},{signal});navigate('/posts');return true}catch(e){error.textContent=e.code==='PASSWORD_MISMATCH'?'비밀번호가 일치하지 않습니다.':e.message;return false}}})};return()=>{};}
